#include <stdio.h> 
// increment operator
// ++i (pre-increment)
// i++ (post-increment)

int main() {
    int i = 1;
    // printf("%d \n", i++); // use, then icrease
    //  printf("%d \n", i);

    printf("%d \n", ++i); // increase, then use 
    printf("%d \n", i);
}