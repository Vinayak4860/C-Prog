// find area of square (side is given and formula is side*side)

#include<stdio.h>

int main() {
    float side;
    printf("Enter Side");
    scanf("%f", &side);
    printf("Area of square is : %f", side*side);
    return 0;
}
